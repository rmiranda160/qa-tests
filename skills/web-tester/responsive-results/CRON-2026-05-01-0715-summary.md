# CRON QA - Responsive Test Report
**Date:** 2026-05-01 07:15 UTC
**Tester Agent:** tester (subagent)
**Scenario:** CMS legal tables (#916) deployment verification + responsive infrastructure scan
**URL:** https://new.zonacnc.com
**Method:** web_fetch + curl (multi-page HTML analysis, no browser available)
**Label:** qa-responsive

---

## Verdict: ❌ FAIL

## Critical Finding: Issue #916 Fix Merged But NOT Deployed

| Check | Status | Detail |
|-------|--------|--------|
| Commit `2d08e26a` on `origin/main` | ✅ PRESENT | Auto-fix #916 merged to main |
| Fix deployed to production `new.zonacnc.com` | ❌ **NOT DEPLOYED** | Production still serves old code |
| Table-responsive wrapper on Privacidad (2 tables) | ❌ **0/2 wrapped** | Raw `<table>` with no responsive handling |
| Table-responsive wrapper on Cookies (1 table) | ❌ **0/1 wrapped** | Raw `<table>` with no responsive handling |
| Table-responsive wrapper on Aviso Legal (1 table, NEW) | ❌ **0/1 wrapped** | Table was added but still not wrapped |

**Root Cause:** Auto-fix batch PR #920 was merged to `origin/main` (commit `2d08e26a`) but the production deployment hasn't picked up the changes. The `main` branch on the production server is behind `origin/main`.

### All 8 CMS Pages Status

| Page | Tables | `table-responsive` | Status |
|------|--------|--------------------|--------|
| `/es/content/7-aviso-legal` | 1 | ❌ | BROKEN on mobile |
| `/es/content/8-privacidad` | 2 | ❌ | BROKEN on mobile |
| `/es/content/9-cookies` | 1 | ❌ | BROKEN on mobile |
| `/es/content/4-como-funciona` | 0 | N/A | OK |
| `/es/content/3-terminos-y-condiciones-de-uso` | 0 | N/A | OK |
| `/es/content/10-preguntas-frecuentes` | 0 | N/A | OK |
| `/es/content/5-pago-seguro` | N/A | N/A | Not tested |
| `/es/content/2-aviso-legal` (if exists) | N/A | N/A | Not tested |

## Responsive Infrastructure Scan

| Check | Status | Detail |
|-------|--------|--------|
| Viewport meta tag | ✅ | Present on all pages: `width=device-width, initial-scale=1` |
| Semantic HTML5 structure | ✅ | `<header>`, `<nav>` (5), `<main>`, `<footer>`, `<section>` (7), `<article>` (6) present |
| CSS assets loaded | ✅ | 22 CSS files loaded including listings.css, theme.css, ux-hotfix |
| Page size (homepage) | 157KB | Acceptable |
| Page size (search) | ~690KB | Large but acceptable |
| All pages HTTP 200 | ✅ | 8/8 pages returned 200 |
| Response times | ✅ | Desktop 0.48s, Mobile 0.51s, Tablet 0.26s |
| Content is consistent across UAs | ✅ | Same HTML delivered regardless of User-Agent (no mobile-specific template) |

## Browser Unavailability

**BLOCKER:** Playwright cannot launch due to missing system libraries (libnspr4, libnss3, libatk-1.0, libcups, libxkbcommon, libasound). MCP browser returned schema error. All visual responsive verification (375px, 768px) was done via HTML/CSS analysis only.

## Recommendations

1. **DEPLOY origin/main to production** — commit `2d08e26a` contains fixes for #916, #675, and 10 other issues
2. **After deploy, re-verify** CMS tables at 375px viewport to confirm responsive behavior
3. **Install Playwright system deps:**
   ```bash
   apt-get install -y libnspr4 libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libxkbcommon0 libasound2
   ```
4. **Fix Aviso Legal table** — this page now has a `<table>` tag that also needs responsive wrapping

---

*Report auto-generated by tester subagent (07:15 UTC batch)*
